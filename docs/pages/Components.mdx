import { PageHeader, Preview } from "@/lib/components";
import * as Examples from "@/lib/examples";

<PageHeader title="Components">
  Fine has styling for basic interactive controls such as inputs, buttons,
  dialogs, navigations, and more.
</PageHeader>

## Button

<Preview
  code={Examples.buttons}
  previewStyle={{
    display: "grid",
    gap: "1rem",
    gridTemplateColumns: "repeat(auto-fill, minmax(12rem, 1fr))",
  }}
/>

## Input

<Preview code={Examples.input} />

Some inputs automatically display an icon based on their type. You can also manually set an icon on an input:

```css
.custom-input {
  --input-has-icon: 1; /* increases padding on the left side */
  --input-icon: url(...); /* URL of the input icon */
}
```

<Preview
  code={Examples.specialInputs}
  previewStyle={{
    display: "grid",
    gap: "1rem",
    gridTemplateColumns: "repeat(auto-fill, minmax(12rem, 1fr))",
  }}
/>

Textareas work almost the same as inputs.

<Preview code={Examples.textarea} />

## Select

<Preview code={Examples.select} />

## Checkbox

<Preview code={Examples.checkbox} />

Give a checkbox the `role="switch"` to make it appear as a switch.

<Preview code={Examples.checkboxSwitch} />

## Radio

<Preview
  code={Examples.radio}
  previewStyle={{
    display: "flex",
    flexDirection: "column",
    gap: "1rem",
  }}
/>

## Dialog

<Examples.Dialog />

## Details

<Preview code={Examples.details} />

## Article

<Preview code={Examples.article} />

## Navigation

Navigations are stacked vertically by default.

<Preview code={Examples.verticalNav} />

Wrap a navigation in a `header` element to create horizontal navigation.

<Preview code={Examples.horizontalNav} />

## Tabs

<Preview code={Examples.tabs} />
